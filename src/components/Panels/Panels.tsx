import { Box } from '@mui/material';
import { PanelTodoList } from './PanelTodoList';

export const Panels = () => {
  return (
    <Box sx={{ position: 'relative' }}>
      <PanelTodoList />
    </Box>
  );
};
